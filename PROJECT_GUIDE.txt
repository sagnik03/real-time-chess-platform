CHESS GAME PROJECT GUIDE
========================

Last updated: 2026-06-18

1) PROJECT OVERVIEW
-------------------
This project is a browser-based chess board implementation using plain JavaScript modules.
It renders an 8x8 board, places all pieces in standard starting positions, and currently
implements click-based movement flow for pawns (with square highlighting).

2) TECH STACK
-------------
- HTML5
- CSS3
- Vanilla JavaScript (ES Modules)
- Static asset loading (SVG images for pieces)
- No frontend framework
- No backend
- No package manager dependency (runs directly in browser)

3) FOLDER AND MODULE GUIDE
--------------------------
Root files:
- index.html: Base page shell and root mount node.
- index.js: App entry point. Initializes rendering and global click events.

Data layer:
- Data/data.js: Creates board state objects (8 rows x 8 columns).
- Data/state.js: Holds and exports global state generated by initGame().
- Data/pieces.js: Piece factories for black/white pieces with image path + piece_name.
- Data/data.json: Small metadata placeholder file.

Render layer:
- Render/main.js:
  - Initializes board DOM.
  - Assigns initial pieces to squares.
  - Renders piece images.
  - Handles square highlight visuals.
  - Moves piece DOM node and syncs data state via moveElement().

Events layer:
- Events/global.js:
  - Global click delegation on root board container.
  - Selects pawn logic by piece type.
  - Computes and renders candidate move highlights.
  - Executes movement to clicked highlight target.

Helpers:
- Helper/constants.js: Shared DOM constants (ROOT_DIV).

Styling:
- style/index.css: Board layout, square colors, highlight styles, and piece image sizing.

Assets:
- Assets/images/pieces/black: Black piece SVGs.
- Assets/images/pieces/white: White piece SVGs.

4) CURRENT IMPLEMENTATION STATUS
--------------------------------
Implemented:
- Dynamic generation of board squares with IDs a1-h8.
- Alternating square colors.
- Initial placement of all chess pieces.
- Click-based interaction for white pawns and black pawns.
- Pawn forward move highlighting:
  - White pawn from rank 2 can move 1 or 2 squares forward.
  - Black pawn from rank 7 can move 1 or 2 squares forward.
  - Otherwise single-square forward highlight.
- Selected piece square self-highlight.
- Move execution updates:
  - Data state (piece reassignment between source/target squares).
  - DOM movement for piece image.
  - Cleanup of prior highlights/selections.

Partially implemented / prepared:
- Pawn diagonal capture variables are declared in white pawn logic but not completed.

Not implemented yet:
- Move legality checks for blocked paths.
- Pawn capture rules (full behavior).
- En passant.
- Promotion.
- Castling.
- Check/checkmate/stalemate logic.
- Turn management (white/black turn order enforcement).
- Move history and undo.
- Rules for non-pawn pieces during gameplay events.

5) HOW THE APP BOOTS
--------------------
1. index.html loads style/index.css and index.js.
2. index.js calls:
   - initGameRender(globalState)
   - GlobalEvent()
3. Render/main.js builds board and places pieces.
4. Events/global.js listens for user clicks and applies movement behavior.

6) KNOWN CODE NOTES
-------------------
- Data/data.js has a Greet() helper that is currently unused.
- In Events/global.js, some temporary variables (for example flatArray or captureIds)
  are declared but not used in all branches.
- Rendering and state are synchronized manually, so future rule additions should keep
  both in sync to avoid visual/state mismatches.

7) RUN INSTRUCTIONS
-------------------
Because this is a static web app, you can run it with any static server or directly open
index.html in a browser. For module loading reliability, serving with a local static server
is recommended.

8) FEATURE UPDATE LOG (APPEND HERE FOR EACH NEW CHANGE)
-------------------------------------------------------
Use this format when a feature is added/updated:

[YYYY-MM-DD] Feature Title
- What changed:
- Files touched:
- Why it was changed:
- Behavior impact:
- Follow-up tasks:

9) LOG ENTRY - THIS UPDATE
--------------------------
[2026-06-18] Added full project guide and maintenance log
- What changed:
  - Created PROJECT_GUIDE.txt with architecture, status, and roadmap notes.
  - Added an append-only feature update log template.
- Files touched:
  - PROJECT_GUIDE.txt
- Why it was changed:
  - To keep a persistent, detailed record of implemented work and future updates.
- Behavior impact:
  - No runtime/app logic changes.
- Follow-up tasks:
  - Append one log entry for every future feature update.
  - Expand this guide as rules and modules evolve.

[2026-06-18] Engine scaffold, centralized state, safe DOM moves, pawn integration
- What changed:
  - Centralized board state and game metadata into `Data/state.js` (`globalState`, `gameState`).
  - Removed direct render calls from data layer: `Data/data.js` `Square.highlight()` now only sets a flag.
  - Added `Data/engine.js` with a basic pawn move generator (`getPawnMoves`) and helpers (`getSquare`, `isEmpty`, `isEnemy`).
  - Updated `Render/main.js` to move `<img>` nodes safely and remove only highlight nodes instead of clearing `innerHTML`.
  - Updated `Events/global.js` to use the engine to compute pawn moves, enforce `gameState.currentTurn`, and record moves into `gameState.moveHistory`.
- Files touched:
  - Data/state.js (modified)
  - Data/data.js (modified)
  - Data/engine.js (added)
  - Render/main.js (modified)
  - Events/global.js (modified)
- Why it was changed:
  - Remove circular import issues and provide a single source of truth for game state.
  - Begin replacing ad-hoc movement logic with a move-generation engine to enable correct rule implementation later.
  - Improve DOM update safety and decouple rendering from data concerns.
- Behavior impact:
  - Pawn selection now comes from a simple engine; highlights show legal pawn targets (no en-passant yet).
  - Only the side whose turn it is (`gameState.currentTurn`) may select pawns.
  - After a successful move, `gameState.moveHistory` is appended and `currentTurn` toggles.
  - Other piece movement and advanced rules remain unimplemented.
- Follow-up tasks:
  - Implement move generators for rook/knight/bishop/queen/king.
  - Implement legal-move filtering (moves that would expose own king to check).
  - Add promotion UI, en-passant handling, castling rules, and draw detection (threefold, 50-move, insufficient material).
  - Add move undo/redo and a proper move application/rollback API for the engine.

[2026-06-18] Added move generators for all pieces and unified selection
- What changed:
  - Extended `Data/engine.js` to implement basic move generators for rook, bishop, knight, queen, and king (including sliding logic for rook/bishop/queen and knight jumps).
  - Added `getMovesForPiece(board, piece)` to return candidate moves for any piece type.
  - Updated `Events/global.js` to use `getMovesForPiece` so selecting any piece shows its candidate moves (turn enforcement remains in place).
- Files touched:
  - Data/engine.js (modified)
  - Events/global.js (modified)
- Why it was changed:
  - To move beyond pawn-only logic and provide a single place to compute candidate moves for all piece types.
- Behavior impact:
  - Clicking a piece of the current side now highlights candidate target squares for that piece type.
  - These are basic move generators and do not yet account for checks, castling, en-passant, or promotion.
- Follow-up tasks:
  - Implement legal-move filtering that removes moves leaving own king in check.
  - Implement special rules: castling, en-passant, promotion.
  - Add a canonical move-application API with rollback for validation and AI search.

[2026-06-18] Legal-move filtering and check detection
- What changed:
  - Added simulation-based legal-move filtering to prevent moves that would leave the moving side's king in check.
  - Implemented cloning and simulation helpers in `Data/engine.js`: `cloneBoard`, `applyMoveOnBoard`, `isSquareAttacked`, `findKingPosition`, `isKingInCheck`, and `getLegalMovesForPiece`.
  - Updated `Events/global.js` to use `getLegalMovesForPiece` so candidate highlights exclude moves that would leave the king in check.
- Files touched:
  - Data/engine.js (modified)
  - Events/global.js (modified)
- Why it was changed:
  - To enforce basic rule that a player cannot make moves leaving their king in check and to prepare for full check/checkmate detection.
- Behavior impact:
  - Piece selection now shows only legal moves (moves that do not expose own king to capture) according to current engine capability.
  - Note: special rules (castling, en-passant) are still not implemented and may affect legality in edge cases.
- Follow-up tasks:
  - Implement castling logic and include castling in legal move generation.
  - Implement en-passant capture handling and promotion (with UI).
  - Add unit tests for `isKingInCheck` and `getLegalMovesForPiece` with representative positions.

[2026-06-19] Castling detection and integration
- What changed:
  - Implemented castling consideration in `Data/engine.js`'s `getLegalMovesForPiece` when passed `gameState`.
  - Castling is allowed when king and corresponding rook have not moved (checked via `gameState.moveHistory`), path squares are empty, the king is not currently in check, and the squares the king passes through are not attacked.
  - Updated `Events/global.js` to pass `gameState` when requesting legal moves so castling can be suggested as a legal target (e.g., `g1`, `c1`, `g8`, `c8`).
- Files touched:
  - Data/engine.js (modified)
  - Events/global.js (modified)
- Why it was changed:
  - Castling is a core chess rule; adding detection improves move legality and prepares for full game-rule correctness.
- Behavior impact:
  - When king and rook are unmoved and path and attack conditions are met, castling target squares will appear as legal moves.
  - Note: executing a castling move must still cause rook repositioning logic in `moveElement` or via a higher-level move-application function; current `moveElement` moves a single piece and does not yet move the rook automatically. Follow-up required to atomically apply castling.
- Follow-up tasks:
  - Update move application to perform rook reposition when castling is executed (atomic king+rook move).
  - Implement en-passant capture handling and promotion (with UI).
  - Add unit tests for `isKingInCheck`, castling edge cases, and `getLegalMovesForPiece`.

[2026-06-19] Atomic castling move application
- What changed:
  - Implemented atomic castling application in `Events/global.js`'s move handler. When a king executes a castling target (e.g., `e1->g1` or `e1->c1`), the corresponding rook is moved automatically (`h1->f1` or `a1->d1`) both in state and DOM.
  - Both the king and rook moves are recorded in `gameState.moveHistory`. The `lastMove` entry reflects the king's castling move with a `castling` marker.
- Files touched:
  - Events/global.js (modified)
- Why it was changed:
  - To ensure castling is applied atomically so board state and DOM remain consistent and user experience is correct when castling occurs.
- Behavior impact:
  - Selecting a valid castling move now results in both king and rook being repositioned automatically.
  - `moveElement` continues to perform single-piece DOM moves; castling handler invokes it twice to achieve an atomic logical operation.
- Follow-up tasks:
  - Add tests for castling edge cases (rook/king moved previously, squares attacked, blocked path).
  - Add undo/redo support to revert multi-piece moves atomically.

[2026-06-19] En-passant capture support
- What changed:
  - Implemented en-passant generation logic in `Data/engine.js`'s `getPawnMoves`, using `gameState.lastMove` to detect eligible en-passant opportunities.
  - Updated `applyMoveOnBoard` usage in simulations so en-passant captures are handled during legal-move checks.
  - Implemented en-passant handling in `Events/global.js`: when an en-passant capture is executed the captured pawn is removed from both `globalState` and the DOM, and the move history records include `enPassant: true` and captured piece details.
- Files touched:
  - Data/engine.js (modified)
  - Events/global.js (modified)
- Why it was changed:
  - En-passant is a key pawn rule required for correctness and accurate move generation.
- Behavior impact:
  - If an adjacent enemy pawn just moved two squares, the player may capture it en-passant; the engine highlights the correct landing square and executing it removes the captured pawn.
- Follow-up tasks:
  - Add unit tests for en-passant scenarios and edge cases.
  - Implement pawn promotion UI and integration.

[2026-06-19] Fixed non-pawn piece selection
- What changed:
  - Removed the pawn-only click gate in `Events/global.js` so any piece image can now be selected and routed through the shared legal-move flow.
  - Renamed the internal selection handler to reflect that it now handles all piece types, not just pawns.
- Files touched:
  - Events/global.js (modified)
- Why it was changed:
  - The UI bug prevented rook, knight, bishop, queen, and king moves because their clicks were ignored before the move engine could run.
- Behavior impact:
  - All pieces should now highlight legal moves when clicked, subject to turn order and engine rules.
  - Castling remains available through king move selection, and pawn-specific rules still work through the same shared path.
- Follow-up tasks:
  - Verify movement and highlighting in the browser for each piece type.
  - Add tests for click dispatch if a test harness is introduced.

[2026-06-19] Normal capture support
- What changed:
  - Updated `Render/main.js` so `moveElement()` clears the destination square before appending the moving piece image, which allows standard captures to replace the captured piece in both DOM and state.
  - Normal captures now work for any legal move that targets an occupied enemy square.
- What else was fixed:
  - Updated `Events/global.js` so clicking an occupied highlighted target square now executes the move/capture instead of being treated as a fresh selection.
- Files touched:
  - Render/main.js (modified)
  - Events/global.js (modified)
- Why it was changed:
  - Capturing a piece should remove the target piece from the board and place the attacker on that square; this is core chess behavior.
- Behavior impact:
  - Clicking a legal capture square now replaces the captured piece instead of leaving both pieces visually present.
  - This applies to all pieces that already have legal move generation, not just pawns.
- Follow-up tasks:
  - Add tests for capture handling and DOM/state synchronization.
  - Consider centralizing capture handling in a dedicated move-application helper to better support undo/redo.

[2026-06-19] Pawn promotion support
- What changed:
  - Added automatic pawn promotion in `Events/global.js` after each move resolves.
  - When a pawn reaches the last rank, it is promoted to a queen of the same color, updating both the underlying square piece and the rendered image source/alt text.
- Files touched:
  - Events/global.js (modified)
- Why it was changed:
  - Pawn promotion is a core chess rule and was the next missing piece of basic move correctness.
- Behavior impact:
  - White pawns reaching rank 8 and black pawns reaching rank 1 now automatically become queens.
  - Promotion is currently automatic; no UI choice is shown yet.
- Follow-up tasks:
  - Replace automatic queen promotion with a promotion picker UI.
  - Add tests for promotion on both colors and for promotion after capture.

[2026-06-19] Responsive board and accessibility pass
- What changed:
  - Redesigned `style/index.css` to make the board responsive with viewport-aware sizing, improved contrast, panel framing, and focus-visible styling.
  - Added keyboard-focus-friendly square styling and a more polished page background.
  - Updated `Render/main.js` to add ARIA roles/labels to squares, `tabindex` for focusability, and descriptive `alt` text for piece images.
  - Marked highlight indicators as decorative with `aria-hidden`.
- Files touched:
  - style/index.css (modified)
  - Render/main.js (modified)
- Why it was changed:
  - To improve usability on smaller screens and make the board more accessible to keyboard and assistive technology users.
- Behavior impact:
  - The board now scales better on different viewport sizes.
  - Squares can receive focus, and pieces expose descriptive labels for screen readers.
- Follow-up tasks:
  - Add visible move history / turn status UI.
  - Consider keyboard move navigation for full accessibility.
  - Add tests or smoke checks for the responsive layout if a UI test harness is introduced.

[2026-06-19] Board sizing and row-gap fix
- What changed:
  - Updated `style/index.css` so the board uses a single viewport-aware size variable and each row/square derives from it.
  - Removed row spacing by eliminating layout gaps and forcing the board container to clip overflow cleanly.
  - Made square width and row height scale from the same board size so the grid remains perfectly proportional on different screens.
- Files touched:
  - style/index.css (modified)
- Why it was changed:
  - The board had visible gaps between rows and was too large on smaller screens; this needed a true responsive grid sizing fix.
- Behavior impact:
  - The chessboard now scales down/up based on the viewport and should preserve a consistent 8x8 grid without row gaps.
- Follow-up tasks:
  - Verify on desktop, laptop, and mobile viewport sizes.
  - Consider adding a max board size preference or zoom control if desired.

[2026-06-19] Turn indicator and move history UI
- What changed:
  - Added a status panel below the board in `index.js` containing a live turn indicator and a move history list.
  - Styled the panel in `style/index.css` to match the board shell and remain responsive alongside the board.
  - Updated `Events/global.js` to sync the turn indicator after each move and append a readable move-history entry for normal moves, captures, castling, and en-passant.
- Files touched:
  - index.js (modified)
  - style/index.css (modified)
  - Events/global.js (modified)
- Why it was changed:
  - To make the app easier to follow during play by showing whose turn it is and what moves have been made.
- Behavior impact:
  - The UI now shows live turn state and a scrollable move history.
  - The panel remains responsive and stacks under the board on smaller screens.
- Follow-up tasks:
  - Add move numbering / algebraic notation if you want a more chess-like history format.
  - Consider keyboard navigation and move selection for accessibility completeness.




